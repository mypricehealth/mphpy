[tool.poetry]
name = "example"
version = "0.1.0"
description = "An example project using mphapi."
authors = ["David Archibald <david@davidarchibald.dev>"]

[tool.poetry.dependencies]
python = "^3.12"
mphapi = { path = "..", develop = true }


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
